# Google Tag Manager container ID. Analytics is gated on this being present AND on a
# production build, so local development never reaches the property.
#
# Local:  copy to .env and fill in
# CI:     set as a GitHub repository VARIABLE named PUBLIC_GTM_ID (not a secret — the
#         ID is public in the page source anyway)
PUBLIC_GTM_ID=
